#include "holberton.h"
/**
 * _strlen - Function that returns the lenght of a string.
 * @s: Char as string
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; x++)
	{
	}
	return (x);
}
