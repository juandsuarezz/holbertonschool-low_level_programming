#include <stdio.h>
/**
 * main - Entry point
 * @argc: variable
 * @argv: Variable
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n",argv[0]);
return (0);
}
